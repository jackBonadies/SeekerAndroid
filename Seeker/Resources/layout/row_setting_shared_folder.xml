<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rowRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="56dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:layout_marginTop="4dp"
    android:layout_marginBottom="4dp"
    android:layout_marginStart="12dp"
    android:layout_marginEnd="12dp"
    android:background="@drawable/shared_folder_row_bg">

    <!-- Inner content carries the dim alpha; the itemView root (rowRoot) is owned by
         the RecyclerView ItemAnimator, which resets its alpha to 1f after add/remove
         animations and would clobber any alpha set there. -->
    <LinearLayout
        android:id="@+id/rowContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="8dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp">

        <ImageView
            android:id="@+id/folderSubdirElbow"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="10dp"
            android:src="@drawable/subdir_elbow"
            android:tint="?attr/normalTextColor"
            android:alpha="0.5"
            android:importantForAccessibility="no"
            android:visibility="gone" />

        <FrameLayout
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="16dp">

            <ImageView
                android:id="@+id/folderStatusIcon"
                android:layout_width="24dp"
                android:layout_height="24dp" />

            <ProgressBar
                android:id="@+id/folderStatusSpinner"
                style="?android:attr/progressBarStyleSmall"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:indeterminate="true"
                android:visibility="gone" />
        </FrameLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/rowTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:textColor="?attr/normalTextColorNonTinted" />

            <TextView
                android:id="@+id/rowSubtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textSize="13sp"
                android:alpha="0.6"
                android:textColor="?attr/normalTextColor"
                android:visibility="gone" />
        </LinearLayout>

        <ImageView
            android:id="@+id/folderEdit"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="8dp"
            android:layout_marginStart="4dp"
            android:src="@drawable/pencil_outline"
            android:tint="?attr/colorPrimary"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/edit_folder" />

        <ImageView
            android:id="@+id/folderRemove"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="8dp"
            android:layout_marginStart="2dp"
            android:src="@drawable/trash_can_outline"
            android:tint="?attr/destructiveColor"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/RemoveFolder" />
    </LinearLayout>
</LinearLayout>
